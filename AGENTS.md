# AGENTS.md

## 目的

シミュレーションの再現性と見た目の自然さを両方守る。内部的に正しくても、実画面で依頼された見た目になっていなければ未完了とする。

## 作業前

- 直近の依頼から「観測された問題」「受け入れ条件」「非対象」を分ける。曖昧な部分を想像で実装しない。
- production変更前に `docs/architecture.md` と `docs/testing.md` を読む。
- バグ修正と見た目変更では `docs/engineering/agent_harness.md` の該当手順を使う。
- 現在のコード、実行経路、既存の計測手段を先に確認する。症状だけから原因を決めない。

## 実装

- KISSを優先し、局所修正で済む問題に新しい抽象化、互換経路、設定フラグを増やさない。
- 症状を隠す補償変更を重ねない。新しい違和感が出たら、まず直前の変更を縮小または戻す。
- 描画上の問題を別要素の色変更で相殺しない。対象、深度、アルファ、描画順、ポストプロセスのどこが原因かを分ける。
- シミュレーション変更と描画変更を同じ作業単位に混ぜない。
- 計測コードは乱数、更新順、浮動小数点演算順、シミュレーション定数を変えない。
- ユーザー所有の未追跡ファイルや無関係な差分へ触れない。

## 検証

- シミュレーション挙動の不変性は Release、seed 1、dt 1/60、1 task のchecksumで判定する。4 taskのchecksumは正当性判定に使わない。
- WASM変更はnativeだけで完了にせず、必要なWASM buildも通す。
- 見た目変更はRelease相当を実ブラウザで確認する。同じviewport、カメラ、個体数、quality設定で変更前後を比較する。
- スクリーンショットは必ず人間が見る。画像を取得しただけで合格にしない。
- 深度・透明・背景に関わる変更は、通常視点だけでなく水面方向、海底方向、Plane外周を確認する。
- Consoleの新規errorとWebGL shader errorがないことを確認する。

## Commit / push / deploy

- 検証済みの意味的な作業単位ごとにcommitし、別件を未commit差分へ積まない。
- push/deploy前に完全なdiffと `git status --short` を確認する。
- 見た目変更はローカル実画面の受け入れ条件を満たすまでpush/deployしない。
- deploy後は公開URLを再読込し、意図したassetが配信され、ローカルと同じ見た目であることを確認する。

## 報告

観測したこと、変えたこと、確認した条件、残る不確実性を区別する。未確認事項を確認済みとして書かない。
